using UnityEngine;

public class Suri : MonoBehaviour
{
    public int mainType; // 0 = none, 1 = fries, 2 = chicken, 3 = both
    public int cookType; // 0 = undercooked, 1 = cooked, 2 = overcooked
    public bool ketchup; // 0 = none, 1 = have;
    public bool mustaard;
    public bool mayo;
    public bool garlic;
    public bool tomatos;
    public bool cheese;
    public bool spicy;
    public bool pepper;
    public bool wrapper;
    public bool grillMarks;


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
